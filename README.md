# 🍄 Mushroom Classification: Baseline vs AutoML

![Python](https://img.shields.io/badge/Python-3.10-blue.svg)
![MLflow](https://img.shields.io/badge/MLflow-Tracking-green.svg)
![Hydra](https://img.shields.io/badge/Config-Hydra-orange.svg)
![AutoML](https://img.shields.io/badge/AutoML-FLAML-yellow.svg)

Цей репозиторій містить MLOps проект для класифікації грибів (їстівні/отруйні).
Основна мета — демонстрація підходу **Reproducible Research** з використанням локального трекінгу експериментів та автоматичного пошуку моделі.

> **Важливо:** У цьому проекті реалізовано стратегію усунення **Data Leakage**. Ознака `odor` (запах) була примусово видалена з навчальних даних, оскільки вона є тривіальним предиктором. Моделі навчаються виключно на візуальних характеристиках.

## 📂 Структура Проекту

Проект дотримується адаптованої структури Cookiecutter Data Science:

```text
├── configs/               # Конфігурація параметрів (Hydra/YAML)
│   └── main.yaml          # Головний конфіг (шляхи, параметри моделі)
├── data/
│   ├── raw/               # Вихідні дані (mushrooms.csv)
│   └── processed/         # (Опціонально) Оброблені дані
├── mlruns/                # Локальне сховище артефактів та метрик MLflow
├── notebooks/             # Jupyter Notebooks для EDA
├── src/                   # Вихідний код
│   ├── train_baseline.py  # Скрипт навчання Random Forest
│   └── train_automl.py    # Скрипт пошуку моделі через FLAML
├── requirements.txt       # Залежності проекту
└── README.md              # Документація
🚀 Встановлення
Для запуску проекту необхідно встановити залежності. Рекомендується використовувати віртуальне оточення.

Bash

# Встановлення бібліотек
pip install -r requirements.txt
🛠 Запуск Експериментів
Керування параметрами (час навчання, шляхи, гіперпараметри) здійснюється через файл configs/main.yaml. Вам не потрібно змінювати код для налаштування експериментів.

1. Baseline Model (Random Forest)
Запуск ручного пайплайну. Використовує Random Forest як надійний базовий алгоритм.

Bash

python src/train_baseline.py
2. AutoML (FLAML)
Запуск автоматизованого пошуку найкращої архітектури. Обмеження часу задається у конфігу (time_budget).

Bash

python src/train_automl.py
3. Аналіз результатів (MLflow Dashboard)
Щоб порівняти метрики, подивитися графіки (ROC Curve, Confusion Matrix) та параметри:

Bash

mlflow ui
Після виконання команди відкрийте у браузері: http://127.0.0.1:5000

📊 Результати Експериментів
Ми провели порівняння ручного налаштування та AutoML. Обидва підходи показали, що датасет (навіть без запаху) є лінійно роздільним.
Експеримент   Алгоритм  Accuracy   F1 Score     Час (навчання/пошук)   Примітки
Baseline   Random Forest 1.00       1.00              < 1 сек        Ручний вибір
AutoML       XGBoost      1.00      1.00               ~60 сек      Автоматичний вибір